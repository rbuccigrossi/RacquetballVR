# Hardwood map

`client/js/court.js` builds the original seeded 1024 × 1024 maple-plank color map once at startup. It repeats over the court floor and uses a polished material with a static lightbox environment reflection. No external downloads, licensed photographs, or per-frame texture generation are needed. This directory is reserved for future replacement texture files.
